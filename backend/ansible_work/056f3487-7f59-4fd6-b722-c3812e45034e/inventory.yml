all:
  children:

    broker_controllers:
      hosts:

        localhost_1:
          ansible_host: "localhost"
          ansible_port: 2223
          ansible_user: "root"

          ansible_ssh_pass: "tantor123"
          ansible_become_pass: "tantor123"

          ansible_become: true
          node_id: 1
          service_role: "broker_controller"


    ksqldbs:
      hosts:

        localhost_2:
          ansible_host: "localhost"
          ansible_port: 2223
          ansible_user: "root"

          ansible_ssh_pass: "tantor123"
          ansible_become_pass: "tantor123"

          ansible_become: true
          node_id: 2
          service_role: "ksqldb"


